import React from 'react';
import Header from '../header';

const Countries = () => {
    return (
        <div>
            <Header /> This is countries pages
        </div>
    );
};

export default Countries;
